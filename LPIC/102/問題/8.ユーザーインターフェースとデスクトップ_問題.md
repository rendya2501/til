# ユーザーインターフェースとデスクトップ 白本問題

ユーザーインターフェースは癒しで助かる。  
これなら1日で終わるな。  

---

## 1 ○

ウィンドウシステムのサーバーとして、/usr/bin/Xorgを持つシステムがある。  
systemdの場合にgraphical.target,SysV initの場合にランレベル5で立ち上げたときどうなるか？  

→  
A : グラフィカルログイン画面が表示される。  
この問題、ここまで言っておいてA以外の選択肢があるといえるのか？  

---

## 2 ×

systemdの場合にgraphical.target,SysV initの場合にランレベル5で立ち上げたとき、ログイン画面が表示されます。  
このログイン画面を表示しているプログラムはどれか？1つ。  

→  
D.gnome-terminal  

あぁ、ミスった。  
どうやら超基本的な事だった要だが、すっ飛ばして問題から入ったから間違えちまった。  

ディスプレイマネージャー  
Xの外観の制御がウィンドウマネージャー  
gnome-terminalは端末エミュレータ。  

しかし、ログインするだけの画面ならログインマネージャーでもいいんじゃないかと思うのだが。  

---

## 3 ×

ディスプレイマネージャーが起動するプログラムはどれか？2ツ  

→  
消去法的にCかDしかない。  
B,Cでした。  
B.gnome-session  
C.Xorgサーバー  

ディスプレイマネージャーはユーザー認証が完了するとXサーバー(Xord)とXクライアントを起動する。  
GNOMEの場合はgnome-session。  
KDEの場合はstartkde。  

gnome-terminalはデスクトップ環境にログインした後、ユーザーが起動するXクライアントアプリ。  
ウィンドウマネージャーはgnome-sessionやstartkedが起動するプログラム。  
ここら辺は起動の順番を復習する必要があるか。  

---

## 4 ×

Waylandについての説明で正しいもの。2つ。

→  
×A.GNOMEやKDEに代わるデスクトップ環境  
○B.Xに変わる新しいディスプレイサーバー  
○D.個々のクライアントアプリケーションのウィンドウを合成してスクリーンイメージを生成する  

あくまでディスプレイサーバーの話。X11↔Wayland。  
Dについては完全に知らない。  

---

## 5 ○

Linuxのウィンドウマネージャーはどれか？2つ。

→  
絶対外せないな。  
gdmとxdmはディスプレイマネージャー。  
GNOMEとKDEは統合ディスプレイ環境。  
kwinはわかる。消去法でmetacityも。  

C,D  

→  
当たった。よかった。  
思えば、ディスプレイマネージャーは最後にdmって付くからわかりやすくていいな。  

---

## 6 △

Xサーバーの色深度(色数)を表示するコマンドはどれか？2つ  

→  
そんなコマンドあるんだ。  
というか、色深度を設定する項目自体何だったか。  
DisplayInfoだっけ？
ならxdpyinfoが最もな気がするけど？  

てか、よく見たら2つか。わからん。  
xwininfoでも選んでおくか。  

当たってた・・・。  
でもこの問題、重要度1だからな。  

→

Xサーバーについての情報を表示するコマンド。  
X DisPlaY INFOrmation  
`xdpyinfo`  

クリックした特定のウィンドウ情報を表示することや、「-root」オプションの指定によりルートウィンドウの情報を表示するコマンド  
`xwininfo`  

スクリーンやウィンドウのイメージをファイルに保存するコマンド。  
`xwd`  

スクリーンに表示されているアプリケーションの一覧を表示するコマンド。  
`xlsclients`  

---

## 7 ○

X Window Systemの解像度を変更するために編集する設定ファイルは何か？1つ。  

→  
xorg.conf  

やったばっかしだからね。  
ついでにScreenって項目が解像度の設定だっけか。  

---

## 8 ○

アプリケーションxclockを2台目のモニターに表示する環境変数の設定はどれか？1つ。  

→  
export DISPLAY=:0,1; xclock  

インデックスは0から始まるはず。  
当たった。  

→  

``` txt
DISPLAYの書式  
サーバー名:ディスプレイ番号.スクリーン番号

変数の要素
サーバー名 : リクエストの送り先のXサーバーを指定する(省略するとlocalhost)
ディスプレイ番号 : 同じキーボードとマウスを共有するモニタの集合に対して付けられる番号(通常は0)
スクリーン番号 : モニタに付けられる番号(0は1代目、1は2台目のモニタを指定)
```

モニター1台目  
DISPLAY:0,0  

2台目  
DISPLAY:0,1  

---

## 9 ○

Xサーバーへ出力を送りために、Xクライアントで設定をする環境変数はどれか？  

→  
DISPLAY?  

表現が若干ややこしい。  
Xに関しては、ユーザーが操作するのはサーバー。  
クライアント側に対して、出力はどれにすればいいのか指定する必要があるわけだよな。  
だからクライアントの環境変数DISPLAYを設定する。  

---

## 10 ×

ネットワーク上のあるホストのアプリケーションを多数のユーザーが各々、自分のデスクトップで利用することになった。  
Xクライアントからのリクエストを各デスクトップに表示するため、各デスクトップでアクセス制御を無効にしたい場合、そのコマンドを引数を含めて記述せよ。  

→  
??

×`xhost -config disable`  
○`xhost +`  

→  
なんかコマンドあったっけか？というところから始まった。  
xhostまではよかったがその後の奴は別のと混ざったな。  
`Xorg - configure` これか。  

+はアクセス制御を有効にするんだっけか。-が制御を外すのでなんでもOK状態になる。  
いや、逆だ。各デスクトップでアクセス制御を無効にするってあるから、逆で+で無効、マイナスで有効だ。  
+は全て受け入れる。-は全て受け入れない。  

てか素朴な疑問なのだが、ここでいうホストってのは一体何の事なんだ？
ホストコンピューターだよね？
あぁ、解説してる。

X Window Systemはネットワーク側のウィンドウシステム。  
XクライアントはTCP/IP上でXプロトコルによりXサーバーのサービスを受ける事ができる。  

Xクライアント側の設定:  
環境変数DISPLAYで送り先のXサーバーを指定する。  

Xサーバー側の設定:  
xhostコマンドを実行してクライアントホストからのリクエストを許可する。  

---

## 11 ×

xhostコマンドでXサーバーへのアクセス許可を設定する事によるセキュリティ上の問題点は何か？1つ。  

→  
うーん。ここまで学習した流れからしてC「クライアントからサーバーへのアクセスに制限がなくなる事」かな？  
違った～。

D「クライアントが認証なしにサーバーにアクセスできること」  
まじか。そうなんだ。  

→  
xhostでクライアントからXサーバーへのアクセスを許可する場合、`xhost +client1 +client2`のようにして、ホスト名/IPアドレスに寄るアクセス制限を行うことができる。  
しかし、クライアントの認証は行わない。  
つまり、なりすましされる可能性があるってことか。  
対策としては、xhostによるアクセス許可の設定を行うのではなく、認証を行うsshのポートフォワーディングを利用する等がある。  

---

## 12 ○

X11ポート転送の説明で正しいものはどれか？2つ。  

→  
DISPLAYは関係ない。  
sshの設定は必要。  
暗号化されていないからsshを使って暗号化するって話だったような？  
リモートホストのデスクトップ画面を～は単なるリモートデスクトップ接続だから関係ない。  
ポート転送は総じてSSHがらみの問題。  
A,C  

当たった。ここまで説明して当たっているなら、この問題は十分ということだろう。  

→
X11ポート転送  
リモートのXクライアントホストのX11ポートをsshの通信路を利用してローカルのXサーバーに転送することで、
リモートで実行したXのアプリケーションをローカルホストの画面に表示、操作する事。  
この場合、Xクライアント(sshサーバ)側では環境変数DISPLAYは自動的に設定される。  

---

## 13 ○

XDMCPの説明で正しいもの1つ。  

→  
C「Xサーバーがネットワークを介してディスプレイマネージャーに接続するためのプロトコル」

もっともらしいフルネームを当てたらCしかないかなって思った。
X Window System Display Manager Control Protocol

---

## 14 ×

VNCの説明で正しいものはどれか？2つ。  

→  
×A,D  
○B,D  

A.Xプロトコルによるホスト間通信機能を提供する。  
B.リモートマシンのグラフィカルデスクトップをローカルマシンのデスクトップに表示する。  
D.通信路にsshを使用することでセキュリティを強化できる。  

そうか、こいつに関してはリモートデスクトップ接続のソフトだったか。  
ならBだよな。  

→
Virtual Network Computing  
RFB(Remote Frame Buffer)プロトコルにより、リモートマシンのグラフィカルデスクトップをローカルマシンのデスクトップ内で共有する。  
通信路にsshを使用する事でセキュリティを強化できる。  

---

## 15 ○

標準機能のみがインストールされたリモートのWindows ServerのコンソールをローカルのLinuxのデスクトップに表示できるプロトコルはどれか？1つ。  

→  
RDP  

サービス問題だな。  

---

## 16

仮想マシン(VM)のコンソールをローカルホストの画面に表示し、操作できるプロトコルはどれか？2つ。  

→  
SPICE  
後は知らない。NFSってなんやねん。  

B.VNCだそうで。  
あれ？こいつって何だっけ？リモートデスクトップ接続するための何かだよな？  

VNCはプロトコルで、設定次第ではコンソールも表示できるので答えになるんだと。  

NFSはファイル共有プロトコル。  
X11ポート転送は個々のX11アプリケーションを表示、操作はできるが、リモートホストのコンソールを表示することは出来ない。  

---

## 17 △

Linuxで利用できるアクセシビリティ機能として、オンスクリーンキーボードを表示するコマンドはどれか？  

→  
gokで。  

当たったけど、しらなんだ。  

gok(GNOME Onscreen Keybord)はGNOME2のオンスクリーンキーボード。  
gokコマンドで表示できる。  

orca  
スクリーンリーダーや拡大鏡を提供する。  

---

## 18 ○

目が不自由な人のためにテキストを読み上げるプログラムはどれか？1つ。  

→  
scnreaderはscanreaderと見た。  
読み上げてくれるならspeakって事だよな？  

D.emacspeak  

まぁ、普通に当たったわ。  

→
emacspeak  
Emacs環境に統合化されたスクリーンリーダーのアプリケーション  
Emacsに読みこんだテキストや電子メール等を音声で読み上げる事ができる。  

---

## 19 ○

スムーズなキー入力が困難な人のためのキーボード設定はどれ？3つ。  

→  
A.スティッキー・キー  
B.スロー・キー  
C.バウンス・キー  

D.パブリックキー
これは暗号化や認証に使われる公開鍵の事。  
